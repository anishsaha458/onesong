# requirements.txt — OneSong API v4.8
# essentia is NOT here — it's installed separately in render.yaml after
# all system libs are in place. Installing it via requirements.txt runs
# before apt-get, so the .so loads but immediately fails on missing libsndfile.

fastapi==0.111.0
uvicorn[standard]==0.29.0
PyJWT==2.8.0
bcrypt==4.1.3
python-multipart==0.0.9
httpx==0.27.0
psycopg2-binary==2.9.9
numpy==1.26.4
yt-dlp==2024.5.27
email-validator==2.1.1
pydantic==2.7.1
pydantic-core==2.18.2